### sudo and curl ###

```
apt update && apt install sudo && sudo apt update && sudo apt install curl
```


### serv00 Clear ###
```
curl -O "https://raw.githubusercontent.com/Emokui/Nothing/Zero/Shell/serv00.sh"
chmod +x serv00.sh
./serv00.sh
```
